import React, {Component} from 'react';
import PropTypes from 'prop-types';

function Singup({prop}) {

  return (
    <div>
      <input type="text" />
    </div>
  );
}
Singup.displayName = 'Singup';
Singup.propTypes = {
  
};

export default Singup;
